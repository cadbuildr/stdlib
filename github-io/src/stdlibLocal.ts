/** Wheel filename from `uv build` in the Python package root (parent of `github-io/`); keep version in sync. */
export const LOCAL_STDLIB_WHEEL_FILE = "cadbuildr_stdlib-0.1.0-py3-none-any.whl";

/** URL path segment (under Vite `base`, e.g. `/stdlib/local-stdlib/...` on GitHub Pages). */
export const LOCAL_STDLIB_URL_SEGMENT = "local-stdlib";
